pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
        flutterPluginPortal()
    }
}

buildscript {
    dependencies {
        // This section is optional; you can keep it if you rely on legacy plugins
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.buildDir = '../build'
subprojects {
    project.buildDir = "${rootProject.buildDir}/${project.name}"
}
subprojects {
    project.evaluationDependsOn(':app')
}

tasks.register("clean", Delete) {
    delete rootProject.buildDir
}
